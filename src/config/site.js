/**
 * Site-wide toggles — flip these when you want to show or hide sections.
 */
export const siteConfig = {
  /** Set to `true` to show the Internship tab in the nav and enable the page. */
  showInternshipSection: false,
};
